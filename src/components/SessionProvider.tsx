'use client';

/**
 * @file src/components/SessionProvider.tsx
 * @summary Session provider for next-auth.
 * @journal
 *   - 2025-07-12: Created the session provider.
 */
import { SessionProvider } from 'next-auth/react';

export default SessionProvider;